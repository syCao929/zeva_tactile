# CTE v4 训练核验 — 2026-09-25

结论：现存 `cte-v4-20260924/cte_step_003000.pt` 的数据、训练更新、验证结果及特征来源相互吻合；本次未发现必须将该模型作废重训的错误。早期 CTE 的缓存问题不能忽略，但不能据此判废已重建缓存、重新起训的当前 v4。尚未证明该表示能提高真实机器人任务成功率。

本次只新增审计脚本和结果，CPU 执行，未修改训练/服务代码、模型、共享缓存，也未启动或停止训练作业。

## 1. 实际训练来源

同一个外部日志记录了两次从零训练，并非累计 6000 步：

- 15:48 开始：3000 步，`effect_diversity_weight=0`。
- 16:09 开始：重新随机初始化，3000 步，`effect_diversity_weight=1`。现存 `train.log`、6 个 checkpoint、`metrics.jsonl` 对应第二次。
- `cte_latest.pt` 保存的 step 和全部 optimizer state 的 step 均为 3000；其全部模型权重与 `cte_step_003000.pt` 完全相同且有限。

依据：`logs/cte-v4-20260924.log:320`、`runs/zeva_cte/cte-v4-20260924/train.log:1`、`audit-artifacts/20260925/cte_audit.json:40`。

| 项目 | 本次核实的设置 |
|---|---|
| 数据 | PressButton4Times，101 episodes，15 Hz，11,883 个视觉边界 |
| 输入视觉 | 左相机与腕相机各缩到 256×256，水平拼接；再缩到 480×832，映射到 [-1,1] |
| VAE | 冻结 Wan2.2 VAE，每 4 个控制步的边界图像独立编码，latent 为 48×30×52 |
| 输入动作 | 未归一化的 18 维关节命令；全部缓存与原始 parquet 的 action 逐项相等 |
| 一个窗口 | 17 个 latent 边界、16 个 transition，每个 transition 4 步动作，共 64 步，约 4.27 秒 |
| 一个 effect | 4 个 transition，即 16 个控制步，约 1.07 秒；每个完整窗口 4 个 effect |
| 数据划分 | 96 集训练、5 集验证，episode ID 无交集；9748/519 个窗口 |
| 验证集 | episode 18、21、33、56、59；划分 seed 默认 42 |
| 网络 | 约 9.49M 参数，hidden=256，4 层 GRU 交互块，phase/effect 各 128 维，非 Mamba |
| 优化 | AdamW，lr=1e-4，weight_decay=0.05，batch=8，3000 updates，每 500 步保存/验证 |
| 数值精度 | 参数 fp32，训练前向 CUDA bf16 autocast，loss fp32；梯度裁剪默认 1.0 |
| EMA | visual target 冻结梯度，decay=0.996，每次 optimizer.step 后更新 |

3000×8=24,000 次窗口采样，约为训练窗口数的 2.46 倍；滑窗互相重叠，不代表 24,000 条独立轨迹。

依据：`cosmos-framework/cosmos_framework/zeva_training/vae_cache.py:114`、`:142`、`:258`；`cte_dataset.py:96`、`:158`；`train_cte.py:96`、`:155`、`:197`、`:234`；`model/zeva/causal_transition_encoder.py:23`。未写入 checkpoint 的 CLI 默认值由现有代码/日志重建，checkpoint 未保存完整命令、代码版本和 loss 配置。

## 2. 学习目标

这是一套视觉与动作的自监督表示训练，不依赖 policy backbone，不使用人工标注的四次按压阶段标签，也不输入触觉。

总损失：

`L = action + vision + 0.2*task + 0.1*phase + 0.25*effect`

- action：从当前因果状态预测下一段 4 步动作，Smooth L1。
- vision：预测下一边界的 EMA visual representation，MSE。
- task：不同任务的语义聚类；当前只有一个 task cluster，此项按设计为零。retrieval head 因而没有任务区分监督。
- phase：鼓励窗口内 phase 沿起止方向前进；这是局部进度约束，不能直接解释为“第几次按按钮”。
- effect：pre 使用当前状态和该 effect 窗口动作，post 只使用窗口起始视觉及实际视觉变化；以固定随机投影的 VAE 变化为目标做对比学习，附加注入码多样性、动作摘要、pre/post 对齐及 VICReg。

第二次训练的 effect 内部权重为：contrastive=1，diversity=1，action=0.05，align=0.1，variance=1，covariance=0.04。记录的 `effect_visual` 仅用于诊断，不直接加入 loss。

pre 头训练时使用窗口动作属于目标设计；服务注入的是已完成窗口的 post。扰动未来视觉和动作后，boundary 8 及之前的 phase、下一段动作预测和已完成 effect 在 CPU 实测中最大变化均为 0，未发现该抽样检查下的未来泄漏。

依据：`cosmos-framework/cosmos_framework/model/zeva/cte_losses.py:161`、`causal_transition_encoder.py:204`、`:297`；`audit-artifacts/20260925/cte_audit.json` 的 `causality_max_abs_difference`。

## 3. 实测验证

- 全部 101 份 latent/action 缓存和 101 份 feature 缓存：shape、有限值、边界时间索引、effect 有效掩码通过；无效 effect 槽均为零。
- 原始视频 episode 0、raw frame 16 重新解码，按单帧路径在 CPU bf16 VAE 重编码：输入范围 [-0.99249,1]，与现存 latent 余弦 0.999973，相对 L2 误差 0.00835。支持当前缓存使用正确缩放及单帧路径。这里是单帧抽样验证，并非重编码整个数据集；CPU/CUDA bf16 数值路径不同。
- 最终模型完整 519 窗口验证：CPU fp32 total=0.697247，原日志 CUDA bf16 total=0.697299；action=0.005072，vision=0.002078。
- 抽样重算六个 feature boundary：phase 余弦均大于 0.999987，effect 最低余弦 0.999846，支持 feature cache 与现存 checkpoint 一致。
- 全数据按 16 个边界间隔抽取 687 个最新 effect：平均两两余弦 0.00950，中心化奇异值熵有效秩 76.24（128 维）；未见常量或 rank-one 坍缩。phase 同批抽样平均余弦 0.77848、有效秩 68.43，不能仅因余弦较高就判为常量。
- 34 个间隔 17 边界的验证窗口中，最终 effect 与固定视觉变化目标的相似度矩阵 Pearson r=0.5761；这是小样本表示诊断，不是成功率。历史 README 的旧 CTE v3 r=0.960 不能套用于当前 v4，本次抽样也不保证与历史统计口径一致。
- 6 个保存点原始验证 total：0.84348 → 0.76039 → 0.74256 → 0.72588 → 0.70461 → 0.69730。3000 是现存点中该验证指标最低者，不等于实机最优点。
- 既有 CTE forward/loss/EMA 与 checkpoint 兼容测试：2 passed。CPU 测试过程中 PyTorch 打印 CUDA 探测警告，未使用 GPU。

结果：`audit-artifacts/20260925/cte_audit.json`、`cte_vae_probe.json`；可复跑脚本为同名 `.py`。

## 4. 发现的问题与影响范围

### P2：验证组批使 total loss 与训练日志不能直接比较

训练 shuffle，验证不 shuffle，因此一个验证 batch 通常是同一 episode 的相邻窗口；相邻 17 帧窗口重叠 16 帧。NCE 将别的样本当负样本，同时 diversity/VICReg 也依赖 batch 组成。

在相同 519 窗口、相同已计算输出上，仅重组 batch：

| 组批 | total | effect contrastive | effect diversity |
|---|---:|---:|---:|
| 原顺序 | 0.69725 | 1.69345 | 0.31591 |
| shuffle seed 42 | 0.40956 | 1.03388 | 0.02206 |
| shuffle seed 43 | 0.41644 | 1.04609 | 0.03096 |
| shuffle seed 44 | 0.41925 | 1.05314 | 0.03564 |

所以不能拿末尾训练单 batch 的 0.3181 与验证 0.6973 直接断言严重过拟合。这也不证明完全没有过拟合。后续宜固定代表性验证 batch，兼报逐样本误差、特征诊断及任务评测；不需要因该评估问题重训现存 CTE。

依据：`cosmos-framework/cosmos_framework/zeva_training/train_cte.py:147`、`model/zeva/cte_losses.py:139`；`audit-artifacts/20260925/cte_validation_batches.json:1`。

### P2：首个请求的 phase 与训练 feature cache 不一致

feature cache 对单帧也运行 CTE，保存非零归一化 phase；XHand server 在 buffer 少于两帧时直接返回零 phase。每次 reset 后首个请求存在该差异，随后完整 buffer 路径一致。这是 Stage-2 train/serve 接口问题，不是 CTE 权重训练作废的理由。建议部署前统一首帧策略。

依据：`cosmos-framework/cosmos_framework/zeva_training/cte_features.py:105`、`:119`；`scripts/action_policy_server_xhand.py:204`、`:399`、`:429`。

### P3：多任务验证时 task loss 被无条件清零

`_task_identity_clustering_loss` 的 guard 包含 `not torch.is_grad_enabled()`，导致任何 no_grad 验证都返回零，即使 batch 有多个语义类别。最小探针：两类各两个样本，开启 grad 得 0.00038624，no_grad 得 0。当前单任务本来就应为零，所以不影响本次 v4；扩展多任务前需要修正。

依据：`cosmos-framework/cosmos_framework/model/zeva/cte_losses.py:69`；`audit-artifacts/20260925/cte_audit.json` 的 `multitask_eval_guard_probe`。

### 历史问题与可解释性限制

- 旧版 `_decode_full_episode` 漏乘 255，使 VAE 输入近乎全黑；旧版整段视频编码也与服务单帧编码不一致。当前代码已修，v4 使用 9 月 24 日重建缓存；本次真实视频抽样复编码支持修复已进入当前产物。依据 `vae_cache.py:114`、`:165`、`logs/vae_cache_v2.log:16`。
- diversity 的平均两两余弦项本身不能保证高秩：平衡的两个相反方向同样能降低均值。因此本次同时核验了有效秩及视觉变化相关性，没有仅凭低余弦宣称表示有效。
- 验证仅 5 个 episode，数据仅一个任务；未审查跨 episode 的近重复录制，也没有真实机器人闭环结果。动作重建、视觉变化编码不能证明长序列中保留了早先的视觉/触觉事件，更不能证明跨任务泛化。
- 当前缓存 manifest 没有记录明确的单帧编码模式、预处理版本或内容 hash；训练 checkpoint 也未保存完整 loss 参数/代码版本。日志与抽样复现能追溯当前产物，长期实验管理仍有改进空间。

## 5. 当前建议

用户明确目标是多种长序列任务上的视觉＋触觉记忆，具体任务的次数/阶段辨识不作为主要评估目标。

继续将 CTE v4 @3000 作为 V3 Stage-2 的局部视觉变化编码器；现有证据不足以要求因训练错误另训 CTE。但“当前 CTE 训练基本正确”不等于“现有系统已具备长时多模态记忆”。

现有实际路径的限制：

- 视觉历史只保留 17 个边界，约 4.27 秒；服务端 deque 会丢弃更早的 latent/action，CTE 每次在该窗口内重新前向，没有把上一窗口的 GRU hidden state 传入。依据 `cosmos-framework/cosmos_framework/scripts/action_policy_server_xhand.py:119`、`:167`、`:399`，以及 `model/zeva/causal_transition_encoder.py:146`。
- 触觉 recipe 设定 30 帧、15 Hz，约 2 秒。BIT forward 裁剪至 memory_steps，GRU 从初始状态处理当前窗口；即使使用其 step API，也只保留最近 30 个 token 再重算。依据 `cosmos-framework/cosmos_framework/configs/base/experiment/action/posttrain_config/action_policy_xhand_zeva_tactile.py:22`、`model/zeva/tactile_memory.py:82`、`:129`。真实策略调用同样走 window forward：`model/generator/mot/cosmos3_vfm_network.py:453`。
- 当前触觉输出作为 effect 残差参与策略条件，不会自动被写回视觉历史或形成持续的联合事件存储。注入与跨窗口保存是两件需要分别验证的事。
- 现行 XHand 触觉路径没有已接通的长时事件库；仓库中存在 PIM 类不能视为当前路径已具备该能力。长任务内记忆也不必依赖跨尝试 PIM，可以采用持续隐状态或任务内事件存储。

因此，后续重点应放在跨窗口记忆的写入、保留、读取和训练，而非单独重复训练同配置 CTE。建议保留现有短时编码器作为基线，选择持续状态或事件级记忆实现任务内长时保留，并让训练看到写入事件到读取事件之间的真实历史。不能仅在部署端增大窗口或保留状态，否则会引入训练/服务不一致。

评估至少包括：

1. 当前观测相近、早先接触/视觉历史不同、所需动作不同的样本；检查策略是否使用历史。
2. 保持当前观测不变，分别清空、打乱、错配视觉与触觉历史；比较后续决策和闭环表现。
3. 扫描事件到决策的间隔，覆盖现有 2 秒/4.27 秒窗口以内及明显超出窗口的情况。
4. 对照无历史、视觉历史、触觉历史、联合历史，并在多任务和未参与训练的任务上验证。

部署前仍需统一首帧 phase 处理，并改进验证报告口径；这两项不替代长时记忆架构和训练的补齐。
