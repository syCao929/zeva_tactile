# V2 清理记录 — 2026-09-25

按用户明确授权，退役并清理本机 V2-proprio 及其派生模型。

- 停止 V2 推理服务 PID126596。GPU查询确认其进程已不在计算进程列表，GPU0显存占用降为0MiB，释放原约31GB显存。
- 删除 `runs/zeva/action_xhand/v2-proprio-20260924`（基座4000步）、`runs/zeva/zeva_xhand/action_policy_xhand_zeva`（从V2派生的Stage-2 500步）、`/tmp/zeva-eval`（从V2派生的完整评测checkpoint）。
- 删除对应训练/服务日志、评测临时脚本和输出、`/tmp/cosmos3_action_server/robolab`服务配置。混有旧V1记录的 `logs/zeva-stage2.log`作为退役Stage-2日志整体清理。
- 共删除16项、142个文件，文件逻辑大小384,987,529,518字节，约385GB / 358.55GiB。精确清单、时间和逐项删除状态见 `audit-artifacts/20260925/v2_cleanup.json`。
- 移除XHand的旧 `arm22` state选项及V2专属说明；保留 `joint18` 正式输入与其他通用数据选择。更新评测脚本说明、README和触觉部署文档，后续Stage-2须显式选择实际保存的V3 checkpoint。

V3训练PID131922、其checkpoint、日志和活动标记保留。V3直接从 `models/cosmos3-nano`起训，不依赖已删除的V2。没有中断或改变当前7卡训练。

保留共享CTE v4、`datasets/xhand_cte_cache_v2`、CTE特征和task-context bank、触觉encoder。这里的cache_v2与退役的V2-proprio策略无关。历史审计报告及归档文档仅用于记录，不是可用模型清单。

验证：34项针对性CPU测试通过；相关Ruff、Bash语法、文档示例检查及 `git diff --check`通过。另核验删除路径全部不存在、V3及共享资源仍存在、GPU0释放且V3训练继续推进。

本次没有访问5090远端；本地没有可确认其身份与模型路径的连接配置。
