# 模板：拷贝为 env.local.sh 后按需修改。env.local.sh 不入库。
# env.sh 末尾会自动 source 它（若存在）。

# TactileTTT 采集的原始数据根目录（convert_xhand_dataset.py 的 --source 默认值）。
# 原始数据在 zeva-work 之外，需单独获取。
export XHAND_SOURCE=/path/to/TactileTTT/data/press_button_4_times
